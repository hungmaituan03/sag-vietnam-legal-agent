# Raw sources

Place downloaded official documents here locally. Document `source_url` in your notes.

Git ignores contents of this folder by default (see `.gitignore`) so we do not accidentally publish proprietary or bulky dumps.

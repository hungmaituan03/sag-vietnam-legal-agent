# LLM provider (pick what you use; leave unused blank)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Model for SAG extract + LLM draft answers (and the chat UI)
OPENAI_MODEL=gpt-5.4-mini

# Voyage AI reranker (required for Week 4+)
VOYAGE_API_KEY=

# Optional: vector / SQL backends when not using local defaults
# DATABASE_URL=
# ELASTICSEARCH_URL=
